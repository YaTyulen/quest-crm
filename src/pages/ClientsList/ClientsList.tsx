import { TableClients } from "../../components"
import { Button } from "../../components/ui-kit"

export const ClientsList = () => {
  return (
    <div>
        <Button onClick={() => {}}>Добавить запись</Button>
        <TableClients/>
    </div>
  )
}
